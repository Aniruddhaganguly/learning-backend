<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Capital City Quiz</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>

  <body id="app">
    <form class="container" action="/submit" method="post">
      <% if(locals.wasCorrect==false){ %>
      <h3>
        Total Score:
        <span id="score"> 0 </span>
      </h3>
      <h1 id="countryName">Game over! Final best score: <%= locals.totalScore %></h1>
      <div class="answer-container">
        <input
          type="text"
          name="answer"
          id="userInput"
          placeholder="Enter the capital"
          autofocus
          autocomplete="off"
          disabled
        />
      </div>
      <h1 class="wrong" id="error">✖</h1>
      <a href="/restart" class="restart-button">Restart</a>
      <% } else { %>
      <div class="horizontal-container">
        <h3>
          Total Score:
          <span id="score"> <%= locals.totalScore ? totalScore : "0" %> </span>
        </h3>
      </div>

      <h1 id="countryName"><%=question.country %></h1>
      <div class="answer-container">
        <input
          type="text"
          name="answer"
          id="userInput"
          placeholder="Enter the capital"
          autofocus
          autocomplete="off"
        />
      </div>
      <button type="submit">
        SUBMIT<% if(locals.totalScore>0){ %>
        <span class="checkmark">✔</span>
        <% }  %>
      </button>
      <% } %>
    </form>
  </body>
</html>
